import type { NextConfig } from 'next';
import withNextConfig from '@repo/next-config';

const nextConfig: NextConfig = {
  // 这里可以添加 flow-service 特定的配置
};

export default withNextConfig(nextConfig);
