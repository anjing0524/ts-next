# 文档更新清单 - 2025-07-08

## 今日主要更新

- **更新技术栈版本**: 同步更新 `技术栈版本对照表`，以反映当前项目依赖的最新版本，并将文件名更新为 `技术栈版本对照表-2025-07-08.md`。
- **修正API文档**:
  - 在 `oauth-service-api-index.md` 中，修正了所有端点路径，确保它们都以 `/api/v2` 为前缀。
  - 在 `API设计规范.md` 中，更新了服务端口信息，移除了不存在的服务，并统一了API版本说明。
- **更新部署文档**: 在 `部署运维文档.md` 中，修正了服务端口号，移除了不存在的服务，并更新了相关的 `docker-compose.yml` 和 `prometheus.yml` 配置示例。
- **清理重复路由**: 删除了 `apps/oauth-service/app/.well-known` 目录及其下的重复 `jwks.json` 路由。
- **更新项目README**: 在 `docs/README.md` 中，更新了服务列表、端口信息和核心文档链接。
- **更新架构设计文档**: 在 `docs/design/系统架构设计.md` 中，更新了指向技术栈版本对照表的链接。
- **创建此更新清单**: 开始跟踪每日的文档变更。

## 后续计划

- [ ] 持续完善各微服务的API文档。
- [ ] 补充更详细的部署和运维指南。
- [ ] 审查并更新项目中的其他 `.md` 文件，确保其内容与当前实现一致。
